spring:
  application:
    name: time
  profiles:
    active: local

  config:
    import:
      - optional:classpath:wakatime.yml
      - optional:classpath:swagger.yml

access-token:
  publishing:
    size: 50